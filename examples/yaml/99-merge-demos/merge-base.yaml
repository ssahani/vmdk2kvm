# Modernized example (config-driven): run with
#   sudo python vmdk2kvm.py --config 99-merge-demos/merge-base.yaml

output_dir: ./out
workdir: ./out/work
verbose: 1
checksum: true
fstab_mode: stabilize-all
regen_initramfs: true
flatten: true
compress: true
compress_level: 6
