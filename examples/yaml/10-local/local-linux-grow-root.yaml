# Modernized example (config-driven): run with
#   sudo python vmdk2kvm.py --config 10-local/local-linux-grow-root.yaml

command: local
vmdk: /path/to/linux.vmdk
resize: +20G
to_output: linux-grown.qcow2
compress: true
compress_level: 6
report: local-linux-grow-report.md
