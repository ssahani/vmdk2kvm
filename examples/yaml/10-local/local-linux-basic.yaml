# Modernized example (config-driven): run with
#   sudo python vmdk2kvm.py --config 10-local/local-linux-basic.yaml

command: local
vmdk: /path/to/linux.vmdk
to_output: linux-fixed.qcow2
report: local-linux-report.md
