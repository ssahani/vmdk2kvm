command: daemon
daemon: true
watch_dir: /incoming
output_dir: /out
workdir: /out/work
verbose: 1

# Optional behavior when running as a service
flatten: true
out_format: qcow2
compress: true
enable_recovery: true
